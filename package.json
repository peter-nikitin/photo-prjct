{
  "private": true,
  "engines": {
    "node": ">=22 <23"
  },
  "scripts": {
    "test:js": "node --test tests/js/*.test.js",
    "test:visual": "sh tests/visual/run-in-container.sh test",
    "test:visual:update": "sh tests/visual/run-in-container.sh update",
    "test:visual:inside": "playwright test",
    "test:visual:update:inside": "playwright test --update-snapshots"
  },
  "devDependencies": {
    "@fontsource-variable/inter": "^5.2.8",
    "@playwright/test": "1.61.0",
    "glightbox": "3.3.1"
  }
}
