events {}
http {
  access_log off;
  server {
    listen 8080;
    client_max_body_size 1m;
    client_body_buffer_size 1m;
    location / { proxy_pass http://web:8000; proxy_set_header Host web; }
  }
}
